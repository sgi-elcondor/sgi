{
  "_README": "Hallazgos revisados a mano y aceptados. Clave: 'categoria|resumen'. Valor: motivo de la aceptación (queda impreso en el anexo). Sólo se aceptan hallazgos verificados leyendo el código, nunca para silenciar ruido.",

  "authz|Sin entrada en ROUTE_PERMISSIONS: GET /api/v1/requerimientos/contadores": "Verificado en requerimientos.controller.js:326-360: cada contador se calcula sólo si el caller tiene el permiso del nivel correspondiente (helper `puede()`), devolviendo 0 si no. No expone datos de terceros; no requiere 403 porque un usuario sin permisos recibe contadores vacíos.",

  "authz|Sin entrada en ROUTE_PERMISSIONS: POST /api/v1/uploads/avatar": "Endpoint de auto-servicio: sube el avatar del propio usuario autenticado. Sin identificador ajeno en el payload.",

  "authz|Sin entrada en ROUTE_PERMISSIONS: GET /api/v1/notificaciones": "Auto-servicio: notificaciones.controller filtra por req.usuario.id_usuario.",
  "authz|Sin entrada en ROUTE_PERMISSIONS: PATCH /api/v1/notificaciones/leidas": "Auto-servicio: marca leídas sólo las notificaciones del propio usuario.",

  "authn|Router público por diseño: /api/v1/auth": "Superficie pública documentada (login, registro, reset, verificación). Cada ruta sensible del router aplica verificarToken o rateLimit explícitamente.",
  "authn|Router público por diseño: /api/v1/public": "Catálogo público de la landing (proyectos, lotes, asesores) con Cache-Control 60 s.",

  "rol-inexistente|Nombre de rol usado en código que no existe en condor.roles: auditoria": "DECISIÓN DE ENTREGA (2026-07-29): no se crea el rol. Impacto verificado = nulo: sólo aparece en role-promotion.service.js:15 (lista de roles protegidos, guarda defensiva inofensiva) y en la trazabilidad INV-04 de requerimientos.controller.js, que ya admite admin/gerencia/dueno. La supervisión está cubierta: auditoria_log:leer lo tienen admin, dueno, gerencia y auxiliar_contable. Se corrigió CLAUDE.md, que lo listaba como rol existente.",

  "log-de-depuracion|1 console.log/debugger en src/index.js": "Log de arranque del servidor (puerto de escucha). Observabilidad legítima, no depuración.",
  "log-de-depuracion|1 console.log/debugger en src/services/comisiones.service.js": "Registra el evento de negocio 'comisión causada' con venta y monto acumulado. Es rastro operativo deseado en producción.",
  "log-de-depuracion|1 console.log/debugger en src/services/mora.service.js": "Registra el resultado del cron actualizar_mora en cada corrida. Necesario para diagnosticar el job."
}
